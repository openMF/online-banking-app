import { ToolbarItemDirective } from './toolbar-item.directive';

describe('ToolbarItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolbarItemDirective();
    expect(directive).toBeTruthy();
  });
});
